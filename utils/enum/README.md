# Deprecated – use @simpill/enum.utils

This folder is **legacy** and is not a published package. It duplicates the API now provided by **@simpill/enum.utils**.

- **Canonical package:** `@simpill/enum.utils` (in `utils/enum.utils/`)
- **Do not** import from this folder in new code.
- Migrate any remaining references to `@simpill/enum.utils`; this folder may be removed once unused.
